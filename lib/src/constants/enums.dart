enum ButtonShape { outline, filled }
enum ButtonColorScheme { primary, secondary }
enum ButtonEffectType { gradation, hover, opacity }
enum TextType { Text, TextButton, NoMarginPaddingTextButton }

/// 텍스트 포지션용 [RadioButton], [CheckBox]
enum AgnesTextPosition { left, right, top, bottom }
enum AgnesWidgetPosition { left, right, top, bottom }
enum AgnesGroupType {row, column, wrap}